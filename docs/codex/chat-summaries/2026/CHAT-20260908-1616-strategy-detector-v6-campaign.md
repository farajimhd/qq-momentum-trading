# Strategy execution, independent candle states, and resumable V6 swing-book campaigns

- Chat started: 2026-09-08 16:16:03.518 PDT (America/Vancouver; session metadata 2026-09-08T23:16:03.518Z)
- Chat ended or last activity: Ongoing at summary time, 2026-09-09 PDT
- Summary written: 2026-09-09 16:09 PDT (America/Vancouver)
- Chat/task identifier: `01a0834e-a0f6-7551-8c40-025356273ee8` — Diagnose strategy 118 backtest
- Repository or scope: `D:\TradingCodes\quant-research-workbench`; strategy/backtest, structural indicators, historical V5/V6 books, workstation campaign operations
- Related task-history entries: TASK-0014, TASK-0206, TASK-0207, TASK-0208
- Source completeness: Partial. Current conversation, recent tools, selected commits, source and final V6 reports were reviewed. Early assistant responses and individual strategy-run results are incomplete in the retained context; user requests do not by themselves prove implementation or acceptance.

### Narrative

The user began with Candidate 118 executing differently from instructions, missing changing stop/target paths on open positions, and very slow backtests. This continues [the preceding swing-book/strategy summary](CHAT-20260906-0458-swing-books-hindsight-strategy118.md), not a new profitability baseline. The user wanted fundamental fixes, explicit rules before implementation, and no ticker- or historical-data hardcoding.

The strategy contract evolved through repeated SUGP/JUNS chart audits. Episodes were initially described by a MACD-minus-signal threshold of 25 bps, tracking the maximum candle open/close. Entry required VWAP plus a buffer and clearance above the episode's prior body high; equality was rejected and a 15-bps clearance requested. An early intrabar episode-reset preference was superseded by checking MACD on completed one-second candles. Later intrabar reentry still referenced the frozen maximum of prior completed candles; the current candle could update that high only after closing. This distinction prevents the breakout price from moving its own eligibility threshold.

Protection requirements included an initial 5% stop or an available outer swing low, stops below qualified broken resistance, and real working broker targets. Touching a resistance was insufficient; completed closes above its upper bound supplied break evidence. The user rejected spread-derived structural stop buffers: strategy price-action signals and OMS execution responsibilities must remain distinct. Reentry in the same episode needed protection just below the reclaimed episode high. No second position could be requested while another remained open. After a target fill, the strategy had to wait for the relevant candle close before deciding another entry. Remaining acquisition quantity should continue filling as asks move, until filled or exit conditions invalidate acquisition.

Targets moved from a resistance ladder and observed inter-level gaps toward adaptive episode body/gap sizing, with greater weight on recent candle bodies. Strong candles crossing several resistances should receive room to continue, while exhaustion should tighten protection. JUNS examples exposed excessive spread subtraction and lost break witnesses. The user explicitly corrected an invented three-close requirement: saying the third candle crossed a band did not authorize counting three closes before ratcheting. Commits `5939e6b4`, `fbbd3eb9`, `d93153da`, `12b6630a`, and `9c13dcce` record successive reentry, adaptive-target, stop, witness and frozen-high changes; they are not proof of general profitable behavior.

The user temporarily authorized repeated strategy improvement using SUGP/JUNS, hindsight labels and other periods, then explicitly stopped further strategy testing. Research tooling added causal entry/fill attribution, position charts, equity/drawdown and missed-continuation analysis, reusable replay preparation, and recovery of completed audits. Performance work included evidence reuse, incremental liquidity expiry, structural role filtering and moving configuration preparation off the request event loop. These are identifiable in commits including `14e61052`, `1e6cd202`, `1b84be5a`, `57b48abe`, `975e24e2` and `70bb29fa`. No exhaustive reconstruction of every candidate result or independent holdout acceptance was performed for this summary. The stop-testing instruction remains authoritative; do not restart a search campaign from the earlier 12-hour authorization.

Pullback versus forming-resistance classification became the more important problem. The user wanted every completed candle classified, including candles outside positions, then explicitly separated the detector from the strategy. It should be an indicator on any chart, use both local and global swing structure, handle bullish and bearish conditions, and expose retests, recoveries, candle shapes and progression instead of a long-only MACD gate. Labels belong below candles as configurable rows, not hidden in hover behavior. Repeated missing labels in the Backtest chart around Candidates 178/179 motivated shared service/chart integration rather than page-local calculations.

Commits `37147022`, `fdc7891e`, `7853ec95` and `a5776f58` delivered the independent detector, symmetric classification and label configuration, progression, and causal volume/session-extreme evidence. Core paths are `src/market_engine/structural_detector.py`, `structural_volume.py`, `src/backend/structural_detector_service.py`, and `frontend/src/app/components/StructuralDetector.tsx`. HOD tracking was the main user objective; second/third HOD ranks were exploratory, not a mandatory ranking rule. Volume divergence and pullback recovery should be interpreted as scored evidence, not a guaranteed reversal. Future-looking hindsight labels remain separate supervision. At documentation time, 70 detector, volume, V5 and V6 tests passed; this is current regression evidence, not a new browser or live-strategy acceptance run.

Attention then shifted to the structural source itself. V5 had prioritized resistances; `b5dbcea8` added symmetric support grading and streaming selection optimization. `dce34828` updated historical V5 support/resistance construction to share one SQL scan. The user clarified that permanent V4 candidate history was the wrong carry model: day-one candidates become important V5 levels, only selected end-of-day survivors persist as V6, and the next day starts from those survivors. Weak, redundant, broken and evidence-only candidates must not consume permanent storage. Both support and resistance follow this contract.

`b91a0dc6` implemented stable V6 daily survivor persistence and integration. Candidate evidence is transient; persisted survivors retain compact geometry, identity, counters and causal timestamps, rather than full evidence/member archives. Closing consolidation reaches a stable result, preventing a reconstructed seed from changing simply because selection ran again. Splits preserve causal versions and auditable adjustment boundaries. Earlier flawed consolidation builds were not promoted. Source reads remain certified canonical SIP under `historical-sip-condition-v1`, never raw retained flatfiles.

The final local validation reports were reread for this summary: SUGP database `structure_book_c58ec63aab23` and JUNS `structure_book_1bc48fe01de7`, each with 420 sessions through 2026-09-04, validation `passed`, and report status `built_pending_quality_acceptance`. Elapsed times are 290.97 seconds for SUGP and 322.43 seconds for JUNS. Reports live under `D:\TradingML\runtimes\structure-validation\swing-v6-stable-survivors-20260909\{sugp,juns}`. These were local two-worker measurements, not a 64-worker workstation throughput guarantee; visual quality acceptance remains separate.

`f91fa5e0` introduced the all-tradable campaign. It freezes the latest published tradable universe with listing/security provenance, checks certified source coverage, defers ambiguous or missing identities, schedules larger histories first, and gives each ticker independent logs and daily checkpoints. This is the current tradable population, not a survivorship-free historical universe. A JUNS pilot was deferred for ambiguous published identity; do not bypass that gate merely because a ticker-specific research build succeeded. Small pilots exercised planning, two-worker execution, status, stop and resume, but did not establish all-ticker runtime.

The user requested 64 workers on a 128-core/512-GB workstation and one-second progress. `ea92207d` initially bounded only workers times query threads. A later code audit found four concurrent aggregation queries inside every worker, allowing 64 × 4 × 2 rather than the advertised 128 query threads. `df562ae5` fixed this by making each campaign worker read its eight session chunks sequentially. Other read-session consumers retain their default parallelism. Thirty-one focused tests and an actual SUGP canonical read comparison passed; serial and parallel reads produced the same 99 bars. The implementation is hybrid: vectorized ClickHouse filtering and one-second aggregation, ordered Python structural state updates, and batched ClickHouse persistence. It is not a fully vectorized SQL V6 detector.

The user also caught laptop-only path guards. `b8833ba6` centralized allowed runtime validation, accepted the documented workstation UNC root, defaulted credentials to workstation secrets and corrected commands. An unavailable destination fails rather than redirects. Python must execute on the workstation to use its hardware; writing to its share from the laptop does not move compute there. Automatic approval initially rejected pushes to an unverified external remote. The user explicitly authorized the exact GitHub destination, after which pushes succeeded. Synchronization remained laptop-authoritative, committed-source-only, and after push.

The deployed campaign base is `\\DESKTOP-SAAI85T\Workstation-D\TradingML\codes\quant-research-workbench-df562ae5`; the campaign runtime is `\\DESKTOP-SAAI85T\Workstation-D\TradingML\runtimes\structure-validation\v6-all-df562ae5`. All 2,661 committed files in that base were hash-verified and its launcher imported. Historical all-ticker completion was not claimed. A live storage audit found 109 checked book/session/split tables on `live_market_ssd` and all 137 active parts on that disk, with no misplaced parts. These are point-in-time audit counts. Builders explicitly set the policy and verify both policy and physical placement before writing; default/hdd remains prohibited.

The user then encountered WinError 5 replacing a roughly 5.5-MB manifest on SMB. It was not read-only; concurrent-reader contention was consistent with the failure, though the exact original holder was not identified. `07bdc577` added unique temporary files, flush/fsync, atomic replacement and bounded five-second PermissionError retries. It never truncates or deletes the last published manifest to force progress. Six tests passed, four unrelated opt-in tests skipped, and a held-reader replacement test passed on the actual workstation share. The helper was patched into the existing deployment only after controller/worker locks were verified idle. Its unchanged campaign fingerprint was checked, preserving resume.

Finally, 64 new lines every second made progress unusable. The user explicitly requested the design-terminal-ui skill and Rich panels. `f9fc93f8` added `scripts/swing_campaign_dashboard.py`: a fixed overall status panel/bar, stable numbered worker slots, checkpointed-session bars and percentages, attempt elapsed times, terminal-width columns and N/P paging when height is insufficient. One-second refresh updates in place; redirected output emits compact changing summaries. Reports are cached by file metadata, transiently unreadable reports retain stale-marked snapshots, and detail stays in worker logs. Eight focused tests, compact/wide 64-worker renders and a terminal smoke passed. Controller completion and interruption tests used simulated children, not a new full campaign.

That display update accepts only the exact previous controller hashes, LF or CRLF, while checking every other pinned file unchanged. It is a narrow presentation compatibility bridge, not a general hash bypass. Four dashboard-related files were synced into the existing df562ae5 directory; the atomic-save patch remains there too. Consequently the directory name describes the base snapshot, not every patched file. Idle locks and compatibility were verified; the agent did not start the campaign after deployment. The user now authorized task-history updates, superseding the earlier instruction to leave history untouched.

### Durable decisions

- **Confirmed requirements:** No ticker/data hardcoding; causal closed-candle indicator observations; symmetric support/resistance; explicit prior-completed-candle references for intrabar reentry; mandatory broker protection; strategy signals distinct from OMS fills.
- **Architecture:** V6 carries compact daily survivors only. Canonical SIP is historical authority. Daily markers follow verified book writes. ClickHouse handles aggregation; Python owns ordered state transitions.
- **Operations:** Use `live_market_ssd`, verify actual parts, and fail closed. Cap the campaign at 64 workers and workers × threads ≤128 with one query at a time per worker. This excludes background server work and other applications.
- **Rejected approaches:** Permanent redundant V4/evidence carry, silently adding close-count gates, spread-derived structural thresholds, resetting campaign state to fix display issues, and scrolling 64 lines per heartbeat.
- **Uncertainty:** Strategy robustness, full-population V6 quality/ETA, and sustained 64-worker behavior remain unaccepted. Counts and timings above have limited stated scopes.

### Delivered outcomes

- TASK-0014: versioned execution/protection/reentry corrections, adaptive targets, evidence/performance repairs and replay-audit tooling; profitability remains unproven.
- TASK-0207: independent symmetric candle-state indicator, configurable under-candle labels, progression and volume/HOD evidence; implementation commits and current regression tests cited above.
- TASK-0206: symmetric V5 and stable V6 survivor construction; two 420-session validated books, still pending quality acceptance.
- TASK-0208: bounded tradable-universe launcher, workstation paths, query-budget correction, resumable SMB persistence and Rich dashboard; pushed and patched into the existing workstation deployment.
- Operator instructions: `docs/swing_v6_campaign.md`. Final source commits: `df562ae5`, `07bdc577`, `f9fc93f8`; source sync archives remain under workstation `runtimes\source-sync`.

### Unfinished or hanging work

1. **TASK-0208 — Campaign execution/acceptance:** no final all-ticker result or measured 64-worker ETA. User/operator should resume the same runtime after checking status; investigate failed units with logs and use `--retry-failed` deliberately. Completed tickers skip; interrupted tickers resume verified days. Do not run `plan` again on this directory.
2. **TASK-0206 — Structural quality:** reports explicitly remain pending quality acceptance. Review SUGP/JUNS support/resistance geometry, split dates and cross-session continuity using the V6 builds; do not delete earlier reference data. Full-population certification depends on campaign completion and identity resolution.
3. **TASK-0014 — Trading acceptance:** inspect the actually selected current candidate and effective parameters, then only perform newly authorized tests. Do not resurrect Candidate 118 as the latest policy or restart the stopped strategy search. No live/paper promotion or general profit claim is justified here.
4. **TASK-0208 — Deployment identity:** df562ae5 contains two later patches. Future sync/release tooling must account for this and retain the narrow compatibility verification rather than treating the directory name as a complete content hash.

### Unavailable or incomplete source chats

The app inventory exposed this chat and related repository tasks, including “Review long momentum backtest” (`01a06eb2-3dcb-7580-8cc5-287431048fd3`) and “Diagnose level book campaign” (`01a0715c-0d36-7d62-97f6-0004909c13ee`); their message bodies were not reviewed here. The preceding chat is represented by its linked durable summary. Early missing assistant responses and candidate-specific run outcomes in this chat were not invented from user requests. One summary was created for the requested chat only.

### Handoff to the next chat

Read TASK-0208 and `docs/swing_v6_campaign.md` first for campaign operations, TASK-0206 for structural authority, TASK-0207 for indicator ownership, and TASK-0014 before changing trading behavior. On the workstation, activate its Python environment, enter the patched df562ae5 checkout, set `PYTHONDONTWRITEBYTECODE=1`, and use `python -B scripts/build_swing_book_campaign.py run --runtime <the existing v6-all-df562ae5 path> --progress-seconds 1`. Ctrl+C or `stop` checkpoints before exit. Verify current locks/status before deployment or restart; no running-state claim in this summary substitutes for that check. Preserve source/storage integrity and distinguish code delivery, validated checkpoint construction, visual quality and trading acceptance.
