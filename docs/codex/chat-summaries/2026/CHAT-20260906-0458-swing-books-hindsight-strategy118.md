# Efficient swing books, hindsight supervision, and MACD-gap strategy through Candidate 118

- Chat started: 2026-09-06 04:58:20.502 PDT (America/Vancouver; session metadata 2026-09-06T11:58:20.502Z)
- Chat ended or last activity: Ongoing at summary time
- Summary written: 2026-09-08 16:15 PDT (America/Vancouver)
- Chat/task identifier: `01a07695-7116-7a73-a7b1-dc3d521d8728` — Diagnose slow workstation campaign
- Repository or scope: `D:\TradingCodes\quant-research-workbench`; structural books, ClickHouse, hindsight, strategy runtime, Backtest/Replay and chart presentation
- Related task-history entries: TASK-0206, TASK-0014
- Source completeness: Partial. User history and recent implementation/tool evidence are available; some earlier assistant responses and quantitative historical-build results are absent from the supplied context. Current code and selected commits corroborate implementation, not every earlier reported result.

### Narrative

The chat began with a workstation structural-checkpoint campaign running for over twelve hours and progressing too slowly. The user wanted logs examined before code and cared equally about compute and storage efficiency. Earlier source-evidence caps had produced inaccurate levels, so restoring those caps was not an acceptable optimization. The user stopped the expensive campaign and clarified that the useful product was a historical support/resistance level book, not voluminous intermediate evidence. Deletion of old checkpoints was initially requested, then explicitly postponed until the replacement was proved. Retained reference data must not be deleted merely because the new algorithm exists.

The discussion moved from replaying all events through Rust v18 toward bulk historical computation close to the data. The user proposed Polars-style window and aggregation operations, then preferred ClickHouse to avoid fetching raw events. The agreed persistence model was compact validity intervals for levels, with causal availability and end-of-session retention, rather than every fleeting intraday candidate. Live and replay should calculate intraday state; loading a prior-session book and replaying the current session is the expected backtest behavior. Missing live sessions need repair from certified historical SIP data. Split adjustments must close old surviving versions at the split boundary and create adjusted versions, preserving pre-split as-of queries and an audit trail. Backtests at a session's beginning must not load levels learned later that day.

The user repeatedly challenged deviations from that purpose: recomputing already persisted v18 references, relying on unnecessary execution-clock coverage, moving work out of ClickHouse without explaining it, or presenting dense levels as equivalent merely because a comparison passed. Certified `market_sip_compact.events_YYYY` remains historical authority; delayed/ineligible trades must be excluded under the recovered condition policy. `swing_book_source.py` exposes `historical-sip-condition-v1`, which does not require the execution-clock coverage table. Historical source repair must not reopen flatfiles. These decisions supersede the old task ledger's implication that algorithm-18 checkpoint reconstruction is the only next step.

JUNS and SUGP became the visual validation cases. The first dense books prompted score controls, line-versus-band styling, same-role overlap merging, and normalized prominence experiments. Proximity merging widened and mixed zones unhelpfully; the user returned to overlapping-band merging at load time, not merging support with resistance or integrating the POC merge into construction. `p_norm` thresholds changed repeatedly and are not a universal quality guarantee. Later V5 uses deterministic evidence selection, so an old p_norm slider should not be assumed to govern every consumer.

A session-only swing overlay was introduced before historical reconstruction. Reversal confirmation delays and missed stalls led to further detector work; overproduction then required qualification and lifecycle discipline. Historical major levels should survive sessions, but accumulating every weak level creates an unusable book. Commits document successive compact-book, retention, indexing, source-policy, qualification and intraday-preservation changes: `ce4d633b`, `e6d03df6`, `a4e36297`, `25933937`, `0d22fe61`, and `299dbee0`. The user visually accepted progressively improved JUNS/SUGP books, then requested long-range builds. Exact campaign runtimes, coverage and row counts are not re-established here; inspect build manifests before quoting them.

A deterministic resistance-selection overlay was accepted visually before integration. Commit `aa4fae7a` integrated causal V5 books across replay/live. The historical V5 builder selects inside ClickHouse from verified V4 checkpoints; it is not evidence that every stage of the underlying stateful detector runs as one vectorized query. Relevant sources are `scripts/build_swing_structure_book.py`, `scripts/build_swing_book_v5.py`, `scripts/validate_swing_book_v5.py`, `src/market_engine/resistance_selection.py`, `resistance_selection_sql.py`, `swing_book_v5.py`, and the backend book store/cursor/live modules. A validator rejecting `r:<hash>` resistance IDs had hidden V5 resistances in the chart. Questions about late qualification, gaps in a resistance's visible lifetime, supports receiving symmetric quality treatment, and JUNS history above $10 were not conclusively closed in the recent evidence. The user explicitly punted part of the lifecycle-delay issue.

In parallel, the user explored learning optimal entries/exits. Hindsight began as long-only bid/ask-aware profit maximization, with liquidity, spread, costs, minimum-profit filters and MACD-period merging. The user ultimately separated supervision from executability: label price swings in both directions from MACD-open/closed intervals, without liquidity or spread filtering. Commit `754c518c` records this change. The chart toolbar/popover provides hindsight positions and controls. These labels intentionally use future extrema and are supervision only, never live strategy features. Markov models, reinforcement learning and parameter search were discussed, but the chosen near-term route was causal feature evidence around good/bad entries and tops, then deterministic strategy iteration. No trained model or general profitability claim was delivered by the final work.

Many strategy variants followed. MACD thresholds, completed versus forming candles, R1–R4/HOD ladders, sparse ladders with VWAP fallback, stops on supports or broken resistances, local forming-resistance exits, gap targets, and reentry rules were revised repeatedly. Earlier fixed 30-bps holding rules, slope exits, red-candle gates, exact-four-resistance requirements and support-stop variants are historical experiments, not the final contract. The user wants new rules implemented without stale conditions silently surviving. Candidate numbers identify saved configurations; they are distinct from executor revision 47.

Backtest UX also regressed repeatedly: blank canvases, expired focus links, reconnect restrictions, slow later-session progress, sparse chart updates, and missing open-position protection guides. Recent commits `f6b57c69`, `c3e37652` and `69c3b753` cover presets, causal chart/evidence synchronization and older strategy corrections. The chart consumes the atomic canvas payload rather than overwriting it with a separate activity fetch; publication time is captured before asynchronous projection. SUGP/JUNS presets reduce setup work. These repairs do not establish that every earlier slowdown or crash has been eliminated.

Candidate 117 simplified the strategy to MACD above signal and price above VWAP, retaining volume/liquidity/spread gates, initial 5% protection, lower-band close ratchets, provisional forming-resistance exits, same-period maximum-close reentry, and gap-sized overhead targets. It deliberately removed R-rank, previous-body and green-candle entry gates. `4db551bf` implemented the contract in `src/trading_runtime/v5_macd_gap.py` and fixed a separate presentation defect: immediate reentry sharing the previous exit's millisecond failed strict lifecycle matching and lost its entire plan. A recorded SUGP run `505b3bd2-8148-44e5-a6a9-90fa7b10fc99` then projected plans for all fourteen positions. Candidate 117 ID is `aedc840f-aee7-4ea9-8c10-3b612db7e4a4`.

The user's next SUGP run, `cffa40f7-75b1-4c92-bb71-e7db7174e1ab`, exposed why matching the written contract matters. The apparent 04:03:12 bearish entry was submitted at 04:03:11.898150 ET with MACD 0.0381515899 versus signal 0.0377512871, approximately 1.15 bps bullish. Partial buys continued through 04:03:12.737602. Thus the submission did not violate its intrabar MACD comparison, but the user clarified that a mid-period entry must also reclaim the previous candle-body high. The assistant initially overstated that this was implemented; 117 actually tracked completed closes only and restricted reentries only after exit, with intrabar MACD dips resetting the period.

At 04:05:43.265674, the same run bought SUGP at $3.39 inside a $3.38–$3.40 resistance, initially stopped at $3.22 and targeted $3.52. At 04:05:44 the completed close exceeded the lower bound, calculating a $3.37 stop while still inside the band. A simultaneous provisional forming-resistance event, not that stop, caused the exit. This followed 117's lower-bound rule but demonstrated its weakness. The accepted correction requires an upper-bound close before advancing protection. The user also requested a percentage-based VWAP buffer.

Final implementation `cd10d957` delivers Candidate 118. Every mid-period entry must exceed the prior highest completed 1s `max(open, close)`, including initial entries, not just reentries. A completed bearish MACD candle resets that reference; transient intrabar dips do not. Entry still requires current MACD > signal, price > VWAP plus configurable 10 bps, and existing execution gates. Stop begins 5% below entry and ratchets below a resistance's lower band only after a completed 1s close crosses its upper band. Forming-resistance exits and gap-sized upward targets remain. A new semantic `cancel_entry` intent cancels unfilled acquisition when MACD/VWAP fails and reconciles protection without liquidating held shares. The runtime intercepts cancellation before Portfolio approval.

The final chart investigation found another distinct cause: active broker protection did not participate in initial autoscaling, and the chart froze its price range afterward. Adding autoscale participation alone failed the new offscreen test. The completed fix expands the frozen range only when active entry/stop/target values change; navigation alone retains its scale. A fixture with stop $9 and target $14 outside the candle range now renders both onscreen. The last fixture's optional real-run audit returned no data, so its success is a rendered fixture result, not another real-run audit or a running-backtest acceptance test.

### Durable decisions

- **Confirmed requirements:** Use the latest Candidate 118 rules above; preserve causal event timestamps and completed/forming distinctions. Do not infer a rule from a candle's eventual appearance. No new full backtest was requested for these last patches; the user performs visual testing.
- **Architecture:** Strategy emits intents, Portfolio sizes exposure, OMS owns orders. Canceling acquisition is not selling held inventory. Historical books use certified compact SIP and explicit SSD placement, compact versions and as-of loading. Hindsight future knowledge stays outside execution.
- **Rejected approaches:** Evidence caps that alter levels; blanket proximity merging; raw historical flatfile fallback; silently switching sources; backward-drawing newly qualified levels; treating structural similarity or unit tests as trading acceptance.
- **Assumptions made explicit:** Final period termination is a completed bearish 1s MACD observation; 10-bps VWAP buffer is the accepted proposed default. V5 provisional resistance uses the existing stall detector's three tests at an approached ceiling. It is not proof of a future reversal.
- **Uncertainty:** V5 support symmetry, historical coverage/timing, lifecycle quality and all earlier performance incidents need their own evidence. Candidate 118 is not profitability-validated.

### Delivered outcomes

- Candidate 118: `16aff1b5-8e01-404a-a8c5-578c1a1de3c4`, **Swing V5 MACD gap - body high and confirmed breaks**, profile `swing-v5-macd-gap`, contract `swing-v5-macd-gap-1`.
- Source commits: `4db551bf` and `cd10d957`; configuration saved through the normal immutable candidate service. Earlier candidates remain saved; do not assume a saved candidate reproduces old code without its commit.
- Final checks: 76 tests and five subtests passed across `test_v5_macd_gap.py`, `test_v5_breakout.py`, `test_v5_hod_ladder.py`, and `test_long_momentum_contract.py`; managed TypeScript/Vite build passed. Tests cover mid-period body highs, intrabar reset resistance, VWAP buffer, upper-bound stop progression and cancellation without liquidation.
- Managed chart fixture passed and screenshot inspected. Evidence: `D:\TradingML\runtimes\structure-validation\candidate118-presentation`. Earlier 117 evidence is in the adjacent `candidate117-presentation` directory. No generated output belongs in source.

### Unfinished or hanging work

1. **Activation and testing — TASK-0014.** Latest backend activation was not performed by the agent. Earlier automatic approval rejected restart/push; do not bypass that decision. Obtain authorization as needed, use managed services, verify the loaded code and Candidate 118, then let the user test. Do not stop an active backtest without authorization.
2. **Trading acceptance — TASK-0014.** No Candidate 118 backtest was run. Recheck SUGP 04:03:12 and 04:05:43, body-high eligibility, upper-band ratchets, cancellation/partial fills and forming exits. Recheck JUNS large moves without optimizing against future labels. User owns acceptance; agent owns evidence-led diagnosis.
3. **Presentation acceptance — TASK-0014.** Fixture proves changed offscreen protection visibility, not every live update. Verify actual running-backtest orders, position, clock and chart match, including immediate reentry and strategy navigation. Revisit slowdowns/crashes if reproduced.
4. **Structural quality and coverage — TASK-0206.** Recover exact V4/V5 build manifests, source policy, cutoff, split audit, storage placement and timing for JUNS/SUGP before claiming full January-2025-to-present coverage. Audit support symmetry, JUNS levels above $10, and deferred qualification/lifetime gaps. No deletion or full-universe campaign is authorized by this summary request.
5. **Distribution — both tasks.** Source commits are local in the final implementation evidence; pushes were previously rejected. Verify remote state and obtain allowed publication rather than reporting synchronization. Workstation deployment must follow repository commit/push policy.

### Unavailable or incomplete source chats

The current task's earlier assistant/tool history is incomplete in supplied context; exact earlier build counts, timings and some repairs are intentionally not reconstructed. Repository chat inventory exposes related tasks such as **Review long momentum backtest** (`01a06eb2-3dcb-7580-8cc5-287431048fd3`) and **Diagnose level book campaign** (`01a0715c-0d36-7d62-97f6-0004909c13ee`), but they were not re-reviewed for this summary. See [the earlier contract summary](CHAT-20260904-UNKNOWN-long-momentum-contract-execution-repair.md) for prior context; its entry/stop rules are superseded for Candidate 118.

### Handoff to the next chat

Read TASK-0014/TASK-0206, this summary, `v5_macd_gap.py`, the V5 dispatch in `v5_breakout.py`, cancellation handling in `strategy_engine.py`/`runtime.py`, and `ChartPanel.tsx`. Start with activation status and the selected immutable candidate, not a new algorithm. Preserve all-entry body-high eligibility, completed-bar period resets, upper-bound break confirmation, broker-only order authority and causal book availability. Inspect saved decisions and commands before interpreting chart colors. Keep source/data quality, execution correctness, rendered visibility and profitability as separate acceptance claims.
